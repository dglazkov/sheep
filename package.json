{
  "name": "sheep",
  "version": "0.0.0",
  "description": "sheep: the command a coding agent runs to herd coding agents; pi, running in a cell",
  "type": "module",
  "license": "MIT",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/dglazkov/sheep.git"
  },
  "bin": {
    "sheep": "bin/sheep.js"
  },
  "engines": {
    "node": ">=22.19"
  },
  "dependencies": {
    "@silvia-odwyer/photon-node": "0.3.4",
    "jiti": "2.7.0"
  },
  "optionalDependencies": {
    "bufferutil": "^4.0.1",
    "utf-8-validate": ">=5.0.2",
    "supports-color": "^10.0.0",
    "@mariozechner/clipboard": "0.3.9"
  },
  "sheep": {
    "commit": "1fc8d03",
    "builtAt": "2026-09-07T22:08:27Z",
    "wrangler": "4.129.0"
  },
  "//": "GENERATED BRANCH: `pnpm release` builds it from 1fc8d03 on main; develop there, not here. No `workspaces` and no scripts, deliberately: npm's git installer treats either as \"needs preparation\" and then installs this package into an empty directory (isocan #47). The bundles and the Worker are committed here for the same reason: there is no install-time build to make them."
}
