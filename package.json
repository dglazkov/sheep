{
  "name": "sheep",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "node scripts/test.mjs",
    "deploy": "pnpm --filter @sheep/cell run deploy",
    "deploy:pen": "pnpm --filter @sheep/cell run deploy:pen",
    "release": "node scripts/release.mjs",
    "hermetic": "node scripts/hermetic.mjs",
    "bundle": "node scripts/bundle.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.28.0"
  }
}
