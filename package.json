{
  "name": "sheep",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.33.0",
  "scripts": {
    "build": "pnpm -r build",
    "test": "pnpm -r --workspace-concurrency=1 test",
    "deploy": "pnpm --filter @lamb/cell run deploy",
    "deploy:pen": "pnpm --filter @lamb/cell run deploy:pen"
  }
}
