# Projects

One directory per body of work. A project holds everything about itself:
the ideal it is aiming at (`journey.md`), the mechanism (`design.md`), and
the walk that gets there (`phases.md`). Reading a project end to end is
`ls` and then reading in order; adding a doc to one edits nothing outside
it. A project with one design names it `design.md`; a project with several
names each for what it designs.

**Where each project stands lives in its own primary doc**, in front
matter, so this table cannot be more right than the thing it describes.

**A project is short.** It holds one body of work with an end: a journey
that can be walked, phases that close, a last phase after which nothing
in it waits on work. A project that turns out to be long-lived was cut at
the wrong grain and should have been several. The work on the command the
sheepdog runs continues as more short projects, each named for what it
adds. (The shepherd, 6 Sep 2026.)

| Project | What it is | Where it stands |
| --- | --- | --- |
| [lamb](lamb/) | pi, running in a cell, herded by a sheepdog through `lamb`, now `sheep`. A pi session as the durable half of a coding harness, moved into a Durable Object: transcript and workspace as rows, an in-isolate shell, pi's own client attached over a WebSocket, the same bundle on Cloudflare; celld, a second home, was tried and withdrawn on 5 Sep 2026. [`design.md`](lamb/design.md) is the argument, [`journey.md`](lamb/journey.md) the acceptance suite, [`phases.md`](lamb/phases.md) the walk. The first leg of a longer marathon: execution tiers, sub-agents as cells, and permission prompts answered from anywhere are later legs. | **Frozen 6 Sep 2026; built 5 Sep 2026, the day of the design.** pi's storage and repo conformance suites pass over the cell's SQLite; the four tools run over a workspace table and just-bash in the isolate; the cell drives pi's harness with an alarm as the heartbeat and journey 2's eviction test holds at every transition; pi's own client attaches over a WebSocket through `lamb`, and two terminals share a cell; pi's full suite is green with four small patches. Deployed to Cloudflare 6 Sep; journeys 1 and 2 walked there with a real model, including a turn evicted by a live deploy and resumed by the alarm. Git was built as a facade and withdrawn the same day; it is a program and belongs to pen. Journeys recast 5 Sep around agents as the actors; phase 5, the sheepdog's surface (detach, status, wait, abort, log, `--json`), built and walked on the deployed home the same night: journey 5 in full, three sheep herded by a Claude Code session. Journey 3 walked in full the same night, the shepherd's terminal beside the dog's stream. Nothing waits on work. Its one unwalked step, a second machine and a night for journey 1, is carried by recast. |
| [pen](pen/) | Programs, running for a sheep, so the dog can delegate work that needs a machine. The second leg: execution tiers behind `Shell.exec`, a container that runs `node`, `pnpm`, `git`, and tests over a checkout synced from the workspace rows, and a credential helper so a push never shows the model a token. [`design.md`](pen/design.md) is the argument, [`journey.md`](pen/journey.md) the acceptance suite, [`phases.md`](pen/phases.md) the walk. | **Frozen 6 Sep 2026; planned 5 Sep 2026; the scaffold, the checkout, the router, the real machine, the broker, and the isolate built the same day.** Written the day lamb's git facade was withdrawn; the repository journey lives here now. Pen phase 0 is closed: a hash per workspace row and a manifest in one query, the protocol as one file, an agent that answers `ping` over a socket pair in workerd and as a process in the image, which builds. Pen phase 1, the checkout, is closed too: a hundred files sync both ways by hash through the fake, and the rows are whole at every point the container can die. Pen phase 2, the router and the sentence, is closed: a line runs whole in just-bash or in the container, output streams, a dead container is an honest interruption, and a home with no container is lamb byte for byte. Pen phase 3, a real machine, is built and walked on a local rig (wrangler dev, Docker, a real model): journeys 1 and 3 as written; its deployed walk waits on the Workers Paid plan. Pen phase 4, the repository and the broker, is built and proved with real git in Node and the broker in workerd; its walk waits on a fine-grained token. Pen phase 5, the fresh isolate, is built and proved through the real Worker Loader, read-only over the workspace. The Workers Paid plan went on that evening; `lamb-pen` is deployed and journeys 1, 3, and 4 walked there with a real model, closing phases 3 and 5. Pen phase 6, celld, was withdrawn with celld itself the same night, after its walk found celld dropping the work a socket wakes. Pen phase 7 redeployed the lamb home with all of pen and no binding and walked lamb's journeys 1, 2, 4, and 5 there unchanged, then journey 3 with the shepherd at the second terminal. Journey 2 walked against a real repository with the shepherd's token. celld was withdrawn on 5 Sep 2026. The leg is walked; nothing in pen waits on work or on a person. |
| [recast](recast/) | The command the sheepdog runs is `sheep`, not `lamb`. A short project that renames the CLI (`@sheep/cli`, `bin/sheep.js`), the package scope, the dog's configuration (`~/.sheep/config`, `SHEEP_*`), the cell's secrets, and the homes on Cloudflare (`sheep`, `sheep-pen`); freezes lamb and pen as the history they are; and records the publishing debt. Nothing the dog can do changes. [`design.md`](recast/design.md) is the argument, [`journey.md`](recast/journey.md) the acceptance suite, [`phases.md`](recast/phases.md) the walk. | **Done 6 Sep 2026, planned and closed the same morning.** Every name in the repo is sheep; `sheep` and `sheep-pen` are deployed and journey 1 walked on them with a real model; `lamb` and `lamb-pen` are deleted; lamb and pen are frozen. Nothing waits on work. Its Open roster carries lamb journey 1's second machine and night, and the publishing debt. |
| [pasture](pasture/) | The place a dog puts what every sheep on a repository should know. A `Pasture` Durable Object per pasture: a shared tree mounted read-only at `/pasture` in every sheep born into it (brief, setup script, skills, notes), a repository cloned at birth, secrets the broker and setup read, and a herd view generated from the directory. The one write path is the `pasture` program in a sheep's shell and `sheep pasture put` in the dog's. A sheep born into no pasture is unchanged. [`design.md`](pasture/design.md) is the argument, [`journey.md`](pasture/journey.md) the acceptance suite, [`phases.md`](pasture/phases.md) the walk. | **Done 6 Sep 2026, planned in the morning and closed in the afternoon.** Six phases built and walked the same day: the object and the verbs, the read-only mount and the prompt, the `pasture` program and the herd, birth and the credential, setup and skills, and the walk on `sheep` and `sheep-pen` with a real model against a real repository. Nothing waits on work. Its Open roster carries the GitHub App that replaces the pasture's token, a cache for the tree, the pushed column, the container cap against the idle period, and `sheep log`'s birth line. |
| [collar](collar/) | How a shepherd gives their dog the command. A `release` branch on GitHub built from `main`, installed with npm's git installer and nothing on npm, the pi fork inside two bundles; a home on the dog's own machine with no account behind it; `sheep setup`, a skill that is a doorway, and a guide that ships with the binary; and the release proved by a hermetic install in three rings, package, machine, and dog, before it is pushed and after. [`design.md`](collar/design.md) is the argument, [`journey.md`](collar/journey.md) the acceptance suite, [`phases.md`](collar/phases.md) the walk. | **Done 7 Sep 2026, planned in the morning and closed in the afternoon.** The two bundles, the Worker, and the release script; `sheep home local` in workerd under `~/.sheep`; `sheep setup`, the skill at the repo root, and `sheep --agent-help`; the package ring in the release script's guard and in CI, the machine ring on two Docker images, and the dog ring, Claude Code given only the skill and a sentence, which walked journey 1 twice with a real model for half a dollar each. `release` is on GitHub; `npx github:dglazkov/sheep#release setup` is the way in. Nothing waits on work. The deployed home from the package is the next project. |
| [station](station/) | A home in the cloud, from the package, in one path. `sheep home deploy` puts the pen home on the shepherd's Cloudflare account with two environment variables and no browser, the container's image pulled from a registry the release pushed to; `sheep home join` brings a second machine in with the token on stdin; `sheep home delete` ends it by name; the home reports its build stamp and the CLI warns on skew; an account ring deploys, walks, and deletes a station named for the commit; and the local home rents a container when Docker is present. A sheep without a container is a writer, not a worker, so there is no containerless home to deploy. [`design.md`](station/design.md) is the argument, [`journey.md`](station/journey.md) the acceptance suite, [`phases.md`](station/phases.md) the walk. | **Done 7 Sep 2026, planned and closed the same evening.** The release stamps the Worker and names the pen image on Docker Hub, by digest when the workflow pushed it; `sheep home deploy` puts the pen home on the shepherd's account from the package in under a minute, `sheep home join` brings a second machine in with the token on stdin, and `sheep home delete` ends it by name; the account ring deploys, walks, joins from a container, redeploys, and deletes a station named for the commit; journeys 1, 2, and 4 walked on the shepherd's own `sheep-2` with a real model, the upgrade a rollout deploy waits for and the delete listing what goes. Journey 3 walked in the ring against the scratch repository with the shepherd's token, and the checkout's old homes were deleted on the shepherd's word, so the account holds `sheep-2` alone; the local home rents a container from the registry image when Docker answers, walked through the package ring with `--docker`. Journeys 1 to 6 walked; nothing waits. ⚑ steps taken: a Docker Hub token in the repository's secrets, the shepherd's Cloudflare token and container minutes per ring run, and the deletion of the checkout-deployed homes when the shepherd says so. |
| [kennel](kennel/) | A dog's directory. `sheep` finds `.sheep/` by walking up from the working directory, the way git finds `.git`, and falls back to `~/.sheep`: the config and the local home live there, so a dog in each of several directories has its own sheep, token, and home, and shares nothing with the others but the command and wrangler. `sheep setup` makes the directory a kennel and keeps its token out of git. A station deployed from a kennel is named for it, the directory's name and a counter, minted once and recorded, never a hash. [`design.md`](kennel/design.md) is the argument, [`journey.md`](kennel/journey.md) the acceptance suite, [`phases.md`](kennel/phases.md) the walk. | **Done 7 Sep 2026, planned in the afternoon and closed before evening.** `.sheep/` is found by walking up from the working directory, `~/.sheep` the fallback; `sheep setup` makes it with its `.gitignore` entry and warns when git tracks it; the package ring walks two kennels side by side, and journey 1 walked on this laptop with a real model, two homes on two ports with two tokens. The name rule is pure and proved, and the config keeps `name`. Journeys 1, 2, and 4 walked; journey 3 walked on the account by station phase 1 on 7 Sep 2026, `sheep-2` minted from `~/.sheep`. Nothing waits on work here. |
| [eyes](eyes/) | A sheep can look at what it wrote. Cloudflare Browser Run, the platform's Chromium, reached from the sheep's shell: `look <path>` renders a workspace path through request interception over the rows, with no server anywhere, writes the PNG beside it, and prints the page's errors, console, and accessibility tree. The sheep reads the PNG with the read tool it has. One browser per cell, kept ten minutes; the container's dev server, a shared browser per home, and `--script` are later. [`design.md`](eyes/design.md) is the argument, [`journey.md`](eyes/journey.md) the acceptance suite, [`phases.md`](eyes/phases.md) the walk; [`docs/spikes/eyes`](../spikes/eyes/README.md) is the spike that settled the platform. | **Done: three phases closed, 8 and 9 Sep 2026.** The eyes are built and proved in workerd; a sheep on any home with the binding has `look` in its shell, the PNG in the rows and the report on stdout, and one paragraph in its prompt; journeys 1 and 2 walked on the local home with a real model, journey 3 on the account: `sheep-2` upgraded from the release and looked through, the account ring green with the look on its station. Two open debts: eyes in the machine and dog rings (no linux/arm64 Chrome), and a `sheep` already on a machine's PATH fooling the rings' first step. |
| [serve](serve/) | A sheep can look at its dev server. One flag on `look`: `--serve '<command>'` runs the command in the container with `PORT` set, waits for its port, renders the page from it through the eyes' interception, and stops it, so a server lives for the length of a look and the container is never occupied by a standing one. The forward is two frames on the container's one socket, so the container stays a client of the cell and the proof runs in workerd through the fake; a `frontend` skill for pastures ships in the project. A server kept for a turn, hot reload, and the platform's port binding are later. [`design.md`](serve/design.md) is the argument, [`journey.md`](serve/journey.md) the acceptance suite, [`phases.md`](serve/phases.md) the walk. | **Done: three phases closed, 10 Sep 2026.** A sheep on a home with a container can look at its dev server: `look --serve '<command>' [--port <n>] [<path>]` runs it, renders the page its port serves, and stops it; a server lives for one look and no longer, and the report gains a `server` section. The forward is two frames on the container's one socket, so nothing connects into the container. All four journeys walked: 1 and 2 on the local home with a real model, 3 on the account with `sheep-2` upgraded and the account ring green, 4 in workerd against the fake. The walks changed the design three times — the forward reaches either loopback (a server on `localhost` in the image binds `::1`), the closing line reads one clock, and the account ring waits out the platform's rollout, which runs 105 s past the deploy. Four open debts, the largest a second served look at 2.5 s against journey 1's under-two. |
| [end](end/) | A sheep is ended in one verb. `sheep rm <id>` aborts the open turn, disconnects its terminals, destroys its container, closes its browser, empties its cell's storage, and removes its row from the directory, in that order, so an end interrupted can be asked again; the pasture it was born into stays. Every verb on an ended id is one refusal that names `sheep ls`. [`design.md`](end/design.md) is the argument, [`journey.md`](end/journey.md) the acceptance suite, [`phases.md`](end/phases.md) the walk. | **Phase 0 closed 10 Sep 2026**, the evening serve closed and the project was planned, from the shepherd's issue #1. The mechanism is built and proved in workerd against the fake container and the pool's real Chrome: the cell's end in five steps, the Directory's removal, `DELETE /s/<id>`, one refusal sentence on every `/s/<id>` route; falsified by three mutations. Phase 1, the verb, the docs, the home ring's walk, and the walk on the local home with Docker and a real model, is next; its one ⚑ step is the account ring with its new step. |
