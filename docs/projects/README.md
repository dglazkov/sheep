# Projects

One directory per body of work. A project holds everything about itself:
the ideal it is aiming at (`journey.md`), the mechanism (`design.md`), and
the walk that gets there (`phases.md`). Reading a project end to end is
`ls` and then reading in order; adding a doc to one edits nothing outside
it. A project with one design names it `design.md`; a project with several
names each for what it designs.

**Where each project stands lives in its own primary doc**, in front
matter, so this table cannot be more right than the thing it describes.

| Project | What it is | Where it stands |
| --- | --- | --- |
| [lamb](lamb/) | pi, running in a cell, herded by a sheepdog through `lamb`. A pi session as the durable half of a coding harness, moved into a Durable Object: transcript and workspace as rows, an in-isolate shell, pi's own client attached over a WebSocket, the same bundle on Cloudflare and on celld. [`design.md`](lamb/design.md) is the argument, [`journey.md`](lamb/journey.md) the acceptance suite, [`phases.md`](lamb/phases.md) the walk. The first leg of a longer marathon: execution tiers, sub-agents as cells, and permission prompts answered from anywhere are later legs. | **Built 5 Sep 2026, the day of the design.** pi's storage and repo conformance suites pass over the cell's SQLite; the four tools run over a workspace table and just-bash in the isolate; the cell drives pi's harness with an alarm as the heartbeat and journey 2's eviction test holds at every transition; pi's own client attaches over a WebSocket through `lamb`, and two terminals share a cell; the same bundle runs on a local celld node; pi's full suite is green with four small patches. Deployed to Cloudflare 6 Sep; journeys 1 and 2 walked there with a real model, including a turn evicted by a live deploy and resumed by the alarm. Git was built as a facade and withdrawn the same day; it is a program and belongs to pen. Journeys recast 5 Sep around agents as the actors; phase 5, the sheepdog's surface (detach, status, wait, abort, log, `--json`), built and walked on the deployed home the same night: journey 5 in full, three sheep herded by a Claude Code session. Nothing waits on work. Waiting on a person: a second machine, the TUI half of journey 3, and a two-node celld fleet. |
| [pen](pen/) | Programs, running for a sheep, so the dog can delegate work that needs a machine. The second leg: execution tiers behind `Shell.exec`, a container that runs `node`, `pnpm`, `git`, and tests over a checkout synced from the workspace rows, and a credential helper so a push never shows the model a token. [`design.md`](pen/design.md) is the argument, [`journey.md`](pen/journey.md) the acceptance suite, [`phases.md`](pen/phases.md) the walk. | **Planned 5 Sep 2026; the scaffold, the checkout, the router, the real machine, the broker, and the isolate built the same day.** Written the day lamb's git facade was withdrawn; the repository journey lives here now. Pen phase 0 is closed: a hash per workspace row and a manifest in one query, the protocol as one file, an agent that answers `ping` over a socket pair in workerd and as a process in the image, which builds. Pen phase 1, the checkout, is closed too: a hundred files sync both ways by hash through the fake, and the rows are whole at every point the container can die. Pen phase 2, the router and the sentence, is closed: a line runs whole in just-bash or in the container, output streams, a dead container is an honest interruption, and a home with no container is lamb byte for byte. Pen phase 3, a real machine, is built and walked on a local rig (wrangler dev, Docker, a real model): journeys 1 and 3 as written; its deployed walk waits on the Workers Paid plan. Pen phase 4, the repository and the broker, is built and proved with real git in Node and the broker in workerd; its walk waits on a fine-grained token. Pen phase 5, the fresh isolate, is built and proved through the real Worker Loader, read-only over the workspace. The Workers Paid plan is on; the deployed walks of phases 3 and 5 come next, then pen phase 6, celld. |
